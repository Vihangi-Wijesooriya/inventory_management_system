Options -MultiViews
RewriteEngine On
RewriteBase /

# Serve existing files directly
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route API requests to the front controller
RewriteRule ^api(/.*)?$ api/index.php [L,QSA]
